# 디자인 검토 결과

## 후속 요청: 밀도 조정

사용자 요청에 따라 최초 명세보다 여백을 줄였습니다. 목록 제목/내용은 12px/11px,
행 안쪽 여백은 좌우 8px/4px·상하 0px, 항목 간격은 4px입니다.
기본 Windows 글꼴에서 행 높이는 81px에서 39px로 약 52% 감소합니다.
전체 콘텐츠 간격과 편집·설정 내부 간격은 주로 6px, 버튼·입력칸 상하 패딩도 6px로 줄였습니다.
아래 표는 최초 명세 대응 기록이며 현재 간격·크기는 이 후속 요청이 우선합니다.

`design.md`와 PySide6 구현을 비교했습니다. 기존 구현은 프레임리스 창과 OS 블러를 갖췄지만, 테마 색상과 컴포넌트 형태는 부분적으로만 일치했습니다.

| 항목 | 기존 상태 | 수정 결과 |
| --- | --- | --- |
| 배경 | 회색 배경, 기본 불투명도 52% | 명세의 테마별 RGB, 기본 65%. 기존 사용자 설정은 유지 |
| 카드 | 두 테마에 동일한 흰색 알파·테두리 | 다크 7% / 라이트 75% 표면, 테마별 테두리 |
| 편집·설정 | 별도 카드 구분 없음 | 18px 카드와 24px blur / Y 8px / 검정 18% 그림자 |
| 창 모서리 | 16px | 그리기와 창 마스크 모두 20px |
| 버튼 | 12px 모서리의 사각형에 가까운 형태 | 40px 기준 캡슐, 저장 버튼 반전 색상, hover·pressed·focus 구분 |
| 입력 | 10px 모서리, 상시 테두리, 좁은 좌우 여백 | 14px 모서리, 기본 투명 테두리, 포커스 강조, 10px / 16px 패딩 |
| 간격 | 카드 8px, 편집 8px | 카드·편집·버튼 간격 12px |
| 가독성 | 테마와 맞지 않는 보조색 | 명세의 본문·보조 텍스트 색상, 기존 13–15px 본문 유지 |
| 크기·문구 | 최소 260×540, 한영 혼용 | 최소 340×600, 작업 버튼 한국어 통일 |

## 적용 범위와 남은 차이

- 달력·수치 대시보드·탭·토글은 현재 기능에 없으므로 추가하지 않았습니다. 테마·배경 효과는 각 세 가지 옵션을 캡슐형 라디오 버튼으로 표시합니다.
- 버튼 hover는 즉시 색상이 바뀝니다. 투명도 전환 애니메이션은 아직 적용하지 않았습니다.
- 목록 카드는 테두리로 상태를 구분하며 개별 그림자는 없습니다. 편집·설정 카드에 지정 그림자를 적용했습니다. 작은 하위 컴포넌트의 12px 그림자는 미적용입니다.
- 창 바깥 그림자와 배경 블러는 Windows가 관리하므로 명세의 픽셀 단위 그림자 값으로 고정하지 않았습니다. 블러 미지원 시 기존 불투명 대체 동작을 유지합니다.
- 저장된 불투명도는 덮어쓰지 않습니다. 새 설정 또는 기본값 복원에 65%가 적용됩니다.

## 검증

- Windows에서 기존 단위 테스트 12개 통과.
- Qt 스모크 검사 통과: 원문 복사, CRUD, 설정 저장·취소, 창 상태 전환, 화면 변경, 렌더링 알파.
- 라이트·다크의 편집·설정 화면을 렌더링하고 설정 버튼 잘림 검사 통과. 라이트 설정과 다크 편집 이미지는 직접 확인했습니다.
- 검토 이미지: `build/design-review/`. 창 자체 캡처이므로 데스크톱 배경과 OS 블러가 합성된 최종 외관까지 검증한 것은 아닙니다.
